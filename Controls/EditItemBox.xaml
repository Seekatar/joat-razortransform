<UserControl x:Class="RazorTransform.EditItemBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="root" mc:Ignorable="d" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" d:DesignHeight="30">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Control.FontSize" Value="12"/>
            <Setter Property="Control.FontWeight" Value="Normal"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Control.FontSize" Value="12"/>
            <Setter Property="Control.FontWeight" Value="Normal"/>
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <Button x:Name="btnCopy" DockPanel.Dock="Right" Padding="2" ToolTip="Copy the item" Click="btnCopy_Click">Copy</Button>
        <Button x:Name="btnDel" DockPanel.Dock="Right" Padding="2" ToolTip="Delete the item from the list" Click="btnDel_Click">Del</Button>
        <Button x:Name="btnEdit" DockPanel.Dock="Right" Padding="2" ToolTip="Edit the item" Margin="0,0,2,0" Click="btnEdit_Click">Edit</Button>
        <TextBox x:Name="theTextBox" MinWidth="{Binding ActualWidth, ElementName=btnEdit}" Text="{Binding ItemName, ElementName=root}" Margin="0,0,2,0" IsReadOnly="True" />
    </DockPanel>
</UserControl>