<Window x:Class="RazorTransform.ArrayItemEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RazorTransform"
        Title="Item Edit" Height="450" Width="784" WindowStyle="SingleBorderWindow" 
        ShowInTaskbar="False"
        MinHeight="100" MinWidth="300">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="1,1" StartPoint="0,0">
                <GradientStop Color="AliceBlue"  Offset="0"/>
                <GradientStop Color="#FFFFFFFF" Offset="0.667"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>
        <local:NameValueEdit Grid.Row="0" x:Name="nvEdit"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="_OK" Height="23" HorizontalAlignment="Left" Margin="5" Name="btnOk" VerticalAlignment="Top" Width="75" Click="btnOk_Click"  IsDefault="True" />
            <Button Content="_Cancel" Height="23" HorizontalAlignment="Right" Margin="5" Name="btnCancel" VerticalAlignment="Top" Width="75" IsCancel="True" Click="btnCancel_Click" />
        </StackPanel>


    </Grid>
</Window>
